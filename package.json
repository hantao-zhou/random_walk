{
  "name": "random-walk-web",
  "version": "0.1.0",
  "scripts": {
    "build:wasm": "cd wasm && wasm-pack build --target web --out-dir ../web/pkg",
    "start": "npm run build:wasm && npx http-server web"
  },
  "devDependencies": {
    "http-server": "^14"
  }
}
